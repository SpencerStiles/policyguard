# --- API Backend URL ---------------------------------------------------------
# Used by Next.js rewrites to proxy /api/* requests to the FastAPI backend.
API_URL=http://localhost:8000

# --- Public Variables --------------------------------------------------------
# Optional: direct API URL for client-side calls (if not using the proxy).
# NEXT_PUBLIC_API_URL=http://localhost:8000

# --- Node Environment -------------------------------------------------------
NODE_ENV=development
